'use client'
import React from 'react'

type Props = {
    query: {
        tech: string
        difficulty?: string
    } | {}
}

const QuizContent:React.FC<Props> = ({query}) => {
    return (
        <></>
    )
}

export default QuizContent